# rspack-preview-smoke-test

Minimal smoke test for the Linux x64 glibc Rspack pkg.pr.new preview.

Requires Node.js 22 or later and Corepack.

```bash
git clone https://github.com/hardfist/rspack-preview-smoke-test.git
cd rspack-preview-smoke-test
corepack enable
pnpm install
pnpm test
```
